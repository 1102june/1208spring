# 빠른 배포 요약 (KT Cloud 실제 서버)

## 📋 서버 정보
- **IP**: 210.104.76.139
- **사용자**: root
- **비밀번호**: Ahk3678@ as!@ #

---

## ⚡ 5분 안에 배포하기

### 1. 로컬 빌드 (1분)
```powershell
cd C:\Users\subpa\IdeaProjects\WiseYoung_backend
.\빌드_스크립트.ps1
```

### 2. 서버 접속 (30초)
```bash
ssh root@210.104.76.139
# 비밀번호: Ahk3678@ as!@ #
```

### 3. 서버 초기 설정 (한 번만 실행)

```bash
# Java 17 설치
sudo apt update
sudo apt install openjdk-17-jdk -y

# MariaDB 설치
sudo apt install mariadb-server -y
sudo systemctl start mariadb

# 데이터베이스 생성
sudo mysql -u root -p
```

MariaDB에서:
```sql
CREATE DATABASE wise_young CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
EXIT;
```

### 4. 애플리케이션 디렉토리 준비 (30초)

```bash
mkdir -p /home/root/wiseyoung-backend
cd /home/root/wiseyoung-backend

# 환경 변수 파일 생성
nano .env
```

`.env` 파일 내용:
```bash
DB_USERNAME=root
DB_PASSWORD=Ahk3678@ as!@ #
EMAIL_USERNAME=subpark12@gmail.com
EMAIL_PASSWORD=hfgadjzzkzaohcjc
LH_SERVICE_KEY=
YOUTH_POLICY_SERVICE_KEY=346f78cb-6fd7-435e-af26-5a471734b3ac
# 🔐 암호화된 API 키 (로컬에서 암호화 후 설정)
# 자세한 방법: API_키_암호화_가이드.md 참조
ENCRYPTION_KEY=your-encryption-key-here
GEMINI_API_KEY=your-encrypted-api-key-here
KAKAO_REST_API_KEY=7b587003c2c00f816243e86c138e7fcb
```

### 5. JAR 파일 업로드 (30초)

**Windows PowerShell에서:**
```powershell
scp build\libs\youth-0.0.1-SNAPSHOT.jar root@210.104.76.139:/home/root/wiseyoung-backend/app.jar
```

**또는 자동 배포 스크립트 사용:**
```powershell
.\서버_접속_스크립트.ps1
# 메뉴에서 "6. 전체 배포" 선택
```

### 6. 서비스 시작 (1분)

**서버에서:**
```bash
# Systemd 서비스 파일 생성
sudo nano /etc/systemd/system/wiseyoung-backend.service
```

서비스 파일 내용:
```ini
[Unit]
Description=WiseYoung Backend Service
After=network.target mariadb.service

[Service]
Type=simple
User=root
WorkingDirectory=/home/root/wiseyoung-backend
EnvironmentFile=/home/root/wiseyoung-backend/.env
ExecStart=/usr/bin/java -jar -Dspring.profiles.active=prod /home/root/wiseyoung-backend/app.jar
Restart=always
RestartSec=10

[Install]
WantedBy=multi-user.target
```

저장 후:
```bash
sudo systemctl daemon-reload
sudo systemctl enable wiseyoung-backend
sudo systemctl start wiseyoung-backend
sudo systemctl status wiseyoung-backend
```

### 7. 테스트 (30초)

```bash
# 서버에서 테스트
curl http://localhost:8080/api/health

# 외부에서 테스트
curl http://210.104.76.139:8080/api/health
```

---

## ✅ 배포 완료 체크리스트

- [ ] 서버 접속 성공
- [ ] Java 17 설치
- [ ] MariaDB 설치 및 데이터베이스 생성
- [ ] JAR 파일 업로드
- [ ] 환경 변수 설정
- [ ] Systemd 서비스 생성 및 시작
- [ ] API 테스트 성공
- [ ] 안드로이드 앱 Base URL 변경 (`http://210.104.76.139:8080/`)

---

## 📱 안드로이드 앱 업데이트

**이미 완료됨!** `NetworkModule.kt`가 서버 IP로 설정되었습니다.

```kotlin
private const val BASE_URL = "http://210.104.76.139:8080/"
```

---

## 🔄 업데이트 시 (빠른 방법)

```powershell
# 1. 빌드
.\빌드_스크립트.ps1

# 2. 업로드
.\서버_접속_스크립트.ps1
# 메뉴에서 "2. JAR 파일 업로드" 선택

# 3. 서버에서 재시작
ssh root@210.104.76.139
sudo systemctl restart wiseyoung-backend
```

---

## 🆘 문제 발생 시

### 서비스 로그 확인
```bash
sudo journalctl -u wiseyoung-backend -f
```

### 서비스 상태 확인
```bash
sudo systemctl status wiseyoung-backend
```

### 포트 확인
```bash
sudo netstat -tlnp | grep 8080
```

---

## 📚 상세 가이드

- **실제_서버_배포_가이드.md** - 상세한 배포 절차
- **서버_접속_스크립트.ps1** - 자동 배포 도구

---

## 📞 문의

- **멘토**: 김태석 (tckim@hecto.co.kr, 010-8285-7367)

