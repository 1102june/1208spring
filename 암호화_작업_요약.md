# 🔐 API 키 암호화 작업 요약

## ✅ 완료된 작업

1. ✅ **암호화 유틸리티 클래스 생성** (`CryptoUtil.java`)
   - AES-256 암호화 지원
   - Base64 인코딩/디코딩

2. ✅ **GeminiService 수정**
   - 암호화된 키를 복호화해서 사용
   - 환경 변수에서 `ENCRYPTION_KEY`와 `GEMINI_API_KEY` 읽기

3. ✅ **암호화 도구 생성**
   - `ApiKeyCryptoTool.java`: 명령줄 암호화 도구
   - `API_키_암호화_스크립트.ps1`: PowerShell 자동화 스크립트

4. ✅ **문서 작성**
   - `API_키_보안_가이드.md`: 보안 모범 사례
   - `API_키_암호화_가이드.md`: 상세 사용 가이드
   - `API_키_암호화_간단_방법.md`: 빠른 시작 가이드
   - 배포 가이드 업데이트

---

## 📋 다음 단계

### 1. 로컬에서 새 API 키 암호화

```powershell
# 방법 1: Java 클래스 직접 사용 (권장)
cd C:\WiseYoung_backend
.\gradlew build -x test

# 암호화 키 생성
java -cp "build/classes/java/main" com.example.youth.util.ApiKeyCryptoTool generate

# API 키 암호화
java -cp "build/classes/java/main" com.example.youth.util.ApiKeyCryptoTool encrypt "AIzaSyC2SzM_GRKI0pr1AT4PkT7ID29hIM3hFhw" "생성된_암호화_키"
```

### 2. 서버 .env 파일 업데이트

생성된 두 값을 서버에 설정:

```bash
ENCRYPTION_KEY=생성된_암호화_키
GEMINI_API_KEY=암호화된_API_키
```

### 3. 서버 재시작

```bash
sudo systemctl restart wiseyoung-backend
```

---

## 🔑 새 API 키 정보

- **평문 API 키**: `AIzaSyC2SzM_GRKI0pr1AT4PkT7ID29hIM3hFhw`
- **암호화 필요**: 위 키를 암호화하여 서버에 저장

---

## 📚 참고 문서

- `API_키_암호화_가이드.md` - 상세한 사용 가이드
- `API_키_보안_가이드.md` - 보안 모범 사례
- `API_키_암호화_간단_방법.md` - 빠른 시작 가이드

---

## ⚠️ 중요 사항

1. **암호화 키 (`ENCRYPTION_KEY`)는 절대 노출되면 안 됩니다**
   - 서버 `.env` 파일에만 저장
   - Git에 커밋하지 않기
   - 문서에 실제 키 포함하지 않기

2. **암호화 키를 잃어버리면 복호화할 수 없습니다**
   - 반드시 안전하게 백업

3. **서버에 두 값 모두 설정 필요**
   - `ENCRYPTION_KEY`: 복호화에 사용
   - `GEMINI_API_KEY`: 암호화된 API 키

