# ✅ 서버 배포 준비 완료

## 생성된 값 (서버 .env 파일에 추가)

```bash
# 🔐 암호화 키 (복호화에 사용)
ENCRYPTION_KEY=qm9MfSTvjn8IvepsCn0cAqKXLxm+Nf6da40n0kMN2e4=

# 🔐 암호화된 Gemini API 키
GEMINI_API_KEY=iFfWIWG6SuICN30uyNLwZsh26/+DD9k7/tbBtCNSDycIve0IyNB2DrXSZTLWCC4B
```

---

## 📦 서버 배포 단계

### 1단계: JAR 파일 업로드

```powershell
# 로컬에서 실행
scp build\libs\youth-0.0.1-SNAPSHOT.jar root@210.104.76.139:/home/root/wiseyoung-backend/app.jar
```

### 2단계: 서버 .env 파일 업데이트

서버에 SSH 접속:

```bash
ssh root@210.104.76.139
```

`.env` 파일 편집:

```bash
nano /home/root/wiseyoung-backend/.env
```

다음 두 줄을 추가/수정:

```bash
# 🔐 암호화 키 (복호화에 사용)
ENCRYPTION_KEY=qm9MfSTvjn8IvepsCn0cAqKXLxm+Nf6da40n0kMN2e4=

# 🔐 암호화된 Gemini API 키
GEMINI_API_KEY=iFfWIWG6SuICN30uyNLwZsh26/+DD9k7/tbBtCNSDycIve0IyNB2DrXSZTLWCC4B
```

**중요:** 기존 `.env` 파일의 다른 값들은 유지하고, 위 두 줄만 추가/수정하세요.

### 3단계: 서비스 재시작

```bash
# 서비스 재시작
sudo systemctl daemon-reload
sudo systemctl restart wiseyoung-backend

# 상태 확인
sudo systemctl status wiseyoung-backend

# 로그 확인 (복호화 성공 여부)
sudo journalctl -u wiseyoung-backend -n 50 --no-pager | grep -i "gemini\|복호화"
```

**성공 메시지 예시:**
```
Gemini API 초기화:
  - Base URL: https://generativelanguage.googleapis.com/v1
  - Model: gemini-2.5-flash
  - API Key: AIzaSyC2Sz... (복호화 완료)
```

---

## ✅ 배포 체크리스트

- [ ] JAR 파일 업로드 완료
- [ ] 서버 .env 파일에 ENCRYPTION_KEY 추가
- [ ] 서버 .env 파일에 GEMINI_API_KEY 추가
- [ ] 서비스 재시작 완료
- [ ] 로그에서 복호화 성공 확인
- [ ] 챗봇 테스트 성공

---

## 🆘 문제 발생 시

### 환경 변수 오류

```bash
# .env 파일 확인
cat /home/root/wiseyoung-backend/.env | grep -E "ENCRYPTION_KEY|GEMINI_API_KEY"

# 환경 변수 로드 확인
sudo systemctl show wiseyoung-backend --property=Environment | grep ENCRYPTION_KEY
```

### 복호화 실패

```bash
# 로그 확인
sudo journalctl -u wiseyoung-backend -n 100 --no-pager

# 서비스 재시작
sudo systemctl restart wiseyoung-backend
```

---

**이제 서버 배포를 진행하세요!** 🚀

