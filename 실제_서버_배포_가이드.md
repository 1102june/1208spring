# WiseYoung KT Cloud 실제 서버 배포 가이드

## 📋 서버 정보

- **서버 IP**: 210.104.76.139 (hecto-prt-4)
- **VM 사용자**: root
- **VM 비밀번호**: Ahk3678@ as!@ #
- **서비스 번호**: SVC00006668

⚠️ **보안 주의**: 비밀번호는 안전하게 관리하고, 가능하면 SSH 키 기반 인증으로 전환하세요.

---

## 🚀 빠른 배포 절차

### 1단계: 로컬에서 JAR 빌드

```powershell
cd C:\Users\subpa\IdeaProjects\WiseYoung_backend
.\빌드_스크립트.ps1
```

또는 수동 빌드:
```powershell
.\gradlew clean build -x test
```

빌드된 JAR 파일: `build\libs\youth-0.0.1-SNAPSHOT.jar`

---

### 2단계: 서버 접속 및 초기 설정

#### 2-1. 서버 접속
```bash
# SSH로 서버 접속
ssh root@210.104.76.139
# 비밀번호 입력: Ahk3678@ as!@ #
```

#### 2-2. 기본 도구 설치 확인
```bash
# Java 17 확인
java -version

# Java 17이 없으면 설치
sudo apt update
sudo apt install openjdk-17-jdk -y

# MariaDB 확인
mysql --version

# MariaDB가 없으면 설치
sudo apt install mariadb-server -y
sudo systemctl start mariadb
sudo systemctl enable mariadb
```

---

### 3단계: 데이터베이스 설정

```bash
# MariaDB 보안 설정
sudo mysql_secure_installation

# 데이터베이스 생성
sudo mysql -u root -p
```

MariaDB에서 실행:
```sql
-- 데이터베이스 생성
CREATE DATABASE wise_young CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;

-- 사용자 생성 (root 사용 또는 별도 사용자)
-- 옵션 1: root 사용 (간단하지만 보안상 권장하지 않음)
-- 옵션 2: 별도 사용자 생성
CREATE USER 'wiseyoung'@'localhost' IDENTIFIED BY '강력한_비밀번호_설정';
GRANT ALL PRIVILEGES ON wise_young.* TO 'wiseyoung'@'localhost';
FLUSH PRIVILEGES;
EXIT;
```

---

### 4단계: 애플리케이션 디렉토리 준비

```bash
# 애플리케이션 디렉토리 생성
mkdir -p /home/root/wiseyoung-backend
cd /home/root/wiseyoung-backend

# 디렉토리 구조 생성
mkdir -p logs
mkdir -p config
```

---

### 5단계: JAR 파일 업로드

#### 방법 1: SCP 사용 (Windows PowerShell)

```powershell
# 로컬에서 서버로 JAR 파일 업로드
scp build\libs\youth-0.0.1-SNAPSHOT.jar root@210.104.76.139:/home/root/wiseyoung-backend/app.jar

# 비밀번호 입력: Ahk3678@ as!@ #
```

#### 방법 2: WinSCP 사용
1. WinSCP 실행
2. 호스트: 210.104.76.139
3. 사용자명: root
4. 비밀번호: Ahk3678@ as!@ #
5. JAR 파일을 `/home/root/wiseyoung-backend/` 디렉토리로 업로드

---

### 6단계: 환경 변수 설정

서버에서 환경 변수 파일 생성:

```bash
cd /home/root/wiseyoung-backend
nano .env
```

`.env` 파일 내용 (실제 값으로 변경):
```bash
# 데이터베이스 설정
DB_USERNAME=root
DB_PASSWORD=Ahk3678@ as!@ #  # 또는 별도 DB 사용자 비밀번호

# 이메일 설정
EMAIL_USERNAME=subpark12@gmail.com
EMAIL_PASSWORD=hfgadjzzkzaohcjc

# 공공데이터 API 키
LH_SERVICE_KEY=your-lh-service-key
LH_ENCODING_KEY=lPzs%2BxT8j7c%2B%2FkeCPNXvMnNsIiQo5vkcZuCk7p%2F6g%2FV%2FpntFixwv2NHVJIg3fB7XtBfSpnaaNzFVbEPArspfNQ%3D%3D
LH_DECODING_KEY=lPzs+xT8j7c+/keCPNXvMnNsIiQo5vkcZuCk7p/6g/V/pntFixwv2NHVJIg3fB7XtBfSpnaaNzFVbEPArspfNQ==

# 청년정책 API 키
YOUTH_POLICY_SERVICE_KEY=346f78cb-6fd7-435e-af26-5a471734b3ac

# Gemini API 키
GEMINI_API_KEY=AIzaSyCUchqbpTpMMsTno_PiweocZJxrqBUTfpY

# 카카오맵 API 키
KAKAO_REST_API_KEY=7b587003c2c00f816243e86c138e7fcb
```

파일 저장: `Ctrl + X`, `Y`, `Enter`

---

### 7단계: Systemd 서비스 생성

```bash
sudo nano /etc/systemd/system/wiseyoung-backend.service
```

서비스 파일 내용:
```ini
[Unit]
Description=WiseYoung Backend Service
After=network.target mariadb.service

[Service]
Type=simple
User=root
WorkingDirectory=/home/root/wiseyoung-backend
EnvironmentFile=/home/root/wiseyoung-backend/.env
ExecStart=/usr/bin/java -jar -Dspring.profiles.active=prod /home/root/wiseyoung-backend/app.jar
Restart=always
RestartSec=10
StandardOutput=file:/home/root/wiseyoung-backend/logs/app.log
StandardError=file:/home/root/wiseyoung-backend/logs/error.log

[Install]
WantedBy=multi-user.target
```

저장 후:

```bash
# 서비스 등록
sudo systemctl daemon-reload
sudo systemctl enable wiseyoung-backend

# 서비스 시작
sudo systemctl start wiseyoung-backend

# 상태 확인
sudo systemctl status wiseyoung-backend

# 로그 확인
sudo journalctl -u wiseyoung-backend -f
```

---

### 8단계: 방화벽 설정

```bash
# UFW 방화벽 설정 (있는 경우)
sudo ufw allow 22/tcp    # SSH
sudo ufw allow 8080/tcp  # Spring Boot
sudo ufw enable

# 또는 iptables 사용 시
sudo iptables -A INPUT -p tcp --dport 8080 -j ACCEPT
sudo iptables -A INPUT -p tcp --dport 22 -j ACCEPT
```

---

### 9단계: 서버 접근 테스트

```bash
# 서버에서 직접 테스트
curl http://localhost:8080/api/health

# 또는 외부에서 테스트
curl http://210.104.76.139:8080/api/health
```

---

### 10단계: 안드로이드 앱 설정 변경

**파일**: `app/src/main/java/com/example/app/network/NetworkModule.kt`

```kotlin
object NetworkModule {
    // ⚠️ 서버 IP로 변경 필수!
    private const val BASE_URL = "http://210.104.76.139:8080/"
    
    // ... 나머지 코드 동일
}
```

---

## 📝 배포 확인 체크리스트

- [ ] 서버 접속 성공
- [ ] Java 17 설치 확인
- [ ] MariaDB 설치 및 데이터베이스 생성
- [ ] JAR 파일 업로드 완료
- [ ] 환경 변수 파일 생성
- [ ] Systemd 서비스 생성 및 시작
- [ ] 포트 8080 오픈 확인
- [ ] API 테스트 성공
- [ ] 안드로이드 앱 Base URL 변경

---

## 🔧 관리 명령어

### 서비스 관리
```bash
# 서비스 시작
sudo systemctl start wiseyoung-backend

# 서비스 중지
sudo systemctl stop wiseyoung-backend

# 서비스 재시작
sudo systemctl restart wiseyoung-backend

# 서비스 상태 확인
sudo systemctl status wiseyoung-backend

# 서비스 로그 확인
sudo journalctl -u wiseyoung-backend -f
sudo journalctl -u wiseyoung-backend -n 100  # 최근 100줄

# 애플리케이션 로그 확인
tail -f /home/root/wiseyoung-backend/logs/app.log
tail -f /home/root/wiseyoung-backend/logs/error.log
```

### 애플리케이션 업데이트
```bash
# 1. 서비스 중지
sudo systemctl stop wiseyoung-backend

# 2. 백업
cp /home/root/wiseyoung-backend/app.jar /home/root/wiseyoung-backend/app.jar.backup

# 3. 새 JAR 파일 업로드 (로컬에서)
# scp build\libs\youth-0.0.1-SNAPSHOT.jar root@210.104.76.139:/home/root/wiseyoung-backend/app.jar

# 4. 서비스 시작
sudo systemctl start wiseyoung-backend

# 5. 상태 확인
sudo systemctl status wiseyoung-backend
```

---

## 🆘 문제 해결

### 서비스가 시작되지 않을 때
```bash
# 로그 확인
sudo journalctl -u wiseyoung-backend -n 50

# Java 프로세스 확인
ps aux | grep java

# 포트 확인
sudo netstat -tlnp | grep 8080
sudo lsof -i :8080
```

### 데이터베이스 연결 오류
```bash
# MariaDB 상태 확인
sudo systemctl status mariadb

# 연결 테스트
mysql -u root -p wise_young
```

### 포트 접근 불가
```bash
# 방화벽 확인
sudo ufw status
sudo iptables -L

# 포트 오픈
sudo ufw allow 8080/tcp
```

---

## 🔐 보안 권장 사항

1. **SSH 키 인증 설정** (비밀번호 인증 대신)
2. **데이터베이스 별도 사용자 생성** (root 사용 금지)
3. **환경 변수 파일 권한 설정**
   ```bash
   chmod 600 /home/root/wiseyoung-backend/.env
   ```
4. **정기적인 보안 업데이트**
   ```bash
   sudo apt update && sudo apt upgrade -y
   ```
5. **HTTPS 설정** (SSL 인증서 적용)

---

## 📞 연락처

- **멘토**: 김태석
- **이메일**: tckim@hecto.co.kr
- **연락처**: 010-8285-7367

